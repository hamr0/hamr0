.claude/settings.local.json
__pycache__/
*.pyc
.litectx/

# Agent / IDE scratch — never commit (per LIBRARY_CONVENTIONS §7)
# Ignore .claude/ scratch, but KEEP the tracked reference hook (AGENT_RULES §Guardrails).
.claude/*
!.claude/hooks/
.litectx/
.idea/
